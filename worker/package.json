{
  "name": "woovi-kyc-upload-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --config wrangler-local.toml",
    "deploy": "wrangler deploy --config wrangler-cf.toml",
    "tail": "wrangler tail --config wrangler-cf.toml"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250124.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
